package model;

// The code snippet is defining an enumeration called `EnumAction`. An enumeration is a special type in
// Java that represents a fixed set of constants. In this case, the constants are `ADD`, `REMOVE`,
// `MODIFY1`, `MODIFY2`, `MODIFY3`, and `MODIFY4`. These constants can be used to represent different
// actions or states in a program.
public enum EnumAction {

    ADD, REMOVE, MODIFY1,MODIFY2,MODIFY3,MODIFY4;
}
